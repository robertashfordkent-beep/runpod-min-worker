# Runpod minimal worker

Minimal worker for Runpod GitHub integration testing.

- `runpod.serverless.start()` handler for queue-style workers
- `GET /ping` healthcheck for load balancer workers

This repository is intentionally small so Runpod can index it quickly.
